import { index, integer, real, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";

export const users = sqliteTable("users", { id:text("id").primaryKey(), email:text("email").notNull(), name:text("name").notNull(), passwordHash:text("password_hash"), passwordSalt:text("password_salt"), passwordIterations:integer("password_iterations"), role:text("role").notNull().default("RESIDENT"), emailVerified:integer("email_verified",{mode:"boolean"}).notNull().default(false), blockedAt:text("blocked_at"), createdAt:text("created_at").notNull(), updatedAt:text("updated_at") }, (table)=>[uniqueIndex("idx_users_email").on(table.email)]);
export const disasterEvents = sqliteTable("disaster_events", { id:text("id").primaryKey(), name:text("name").notNull(), status:text("status").notNull(), affectedAreas:text("affected_areas").notNull(), approxLat:real("approx_lat"), approxLng:real("approx_lng"), safetyInfo:text("safety_info"), emergencyGuidance:text("emergency_guidance"), severity:text("severity").notNull().default("ADVISORY"), sourceName:text("source_name"), sourceUrl:text("source_url"), verifiedAt:text("verified_at"), expiresAt:text("expires_at"), startsAt:text("starts_at").notNull(), createdAt:text("created_at").notNull(), updatedAt:text("updated_at") }, (table)=>[index("idx_disaster_events_active_map").on(table.status,table.startsAt,table.approxLat,table.approxLng),index("idx_disaster_events_status_expiry").on(table.status,table.expiresAt)]);
export const helpRequests = sqliteTable("help_requests", { id:text("id").primaryKey(), requesterId:text("requester_id").notNull(), clientRequestId:text("client_request_id"), eventId:text("event_id"), category:text("category").notNull(), publicArea:text("public_area").notNull(), peopleCount:integer("people_count").notNull(), description:text("description").notNull(), urgency:text("urgency").notNull(), contactMethod:text("contact_method").notNull(), status:text("status").notNull().default("OPEN"), acceptedBy:text("accepted_by"), assignedVolunteerId:text("assigned_volunteer_id"), imageKey:text("image_key"), approxLat:real("approx_lat"), approxLng:real("approx_lng"), helperLat:real("helper_lat"), helperLng:real("helper_lng"), etaMinutes:integer("eta_minutes"), etaSource:text("eta_source"), deliveryStartedAt:text("delivery_started_at"), deliveryUpdatedAt:text("delivery_updated_at"), deliveryCode:text("delivery_code"), deliveryCodeCreatedAt:text("delivery_code_created_at"), deliveryCodeHash:text("delivery_code_hash"), deliveryCodeExpiresAt:text("delivery_code_expires_at"), deliveryCodeAttempts:integer("delivery_code_attempts").notNull().default(0), createdAt:text("created_at").notNull(), updatedAt:text("updated_at").notNull() }, (table)=>[uniqueIndex("idx_help_requests_requester_client").on(table.requesterId,table.clientRequestId),index("idx_help_requests_status_created").on(table.status,table.createdAt),index("idx_help_requests_requester_created").on(table.requesterId,table.createdAt),index("idx_help_requests_helper_updated").on(table.acceptedBy,table.updatedAt),index("idx_help_requests_category_status").on(table.category,table.status)]);
export const requestUpdates = sqliteTable("request_updates", { id:integer("id").primaryKey({autoIncrement:true}), requestId:text("request_id").notNull(), authorId:text("author_id").notNull(), status:text("status"), body:text("body").notNull(), createdAt:text("created_at").notNull() }, (table)=>[index("idx_request_updates_created").on(table.createdAt)]);
export const helpOffers = sqliteTable("help_offers", { id:text("id").primaryKey(), requestId:text("request_id").notNull(), helperId:text("helper_id").notNull(), message:text("message").notNull(), status:text("status").notNull().default("PENDING"), createdAt:text("created_at").notNull(), updatedAt:text("updated_at").notNull() }, (table)=>[uniqueIndex("idx_offers_request_helper").on(table.requestId,table.helperId),index("idx_offers_helper_created").on(table.helperId,table.createdAt)]);
export const volunteers = sqliteTable("volunteers", { userId:text("user_id").primaryKey(), name:text("name").notNull(), skills:text("skills").notNull(), areas:text("areas").notNull(), available:integer("available",{mode:"boolean"}).notNull().default(true), completedTasks:integer("completed_tasks").notNull().default(0), updatedAt:text("updated_at").notNull() });
export const organizations = sqliteTable("organizations", { id:text("id").primaryKey(), name:text("name").notNull(), publicArea:text("public_area").notNull(), verified:integer("verified",{mode:"boolean"}).notNull().default(false), contactEmail:text("contact_email").notNull(), createdAt:text("created_at").notNull() });
export const resources = sqliteTable("resources", { id:text("id").primaryKey(), organizationId:text("organization_id").notNull(), eventId:text("event_id"), category:text("category").notNull(), name:text("name").notNull(), quantity:integer("quantity").notNull(), unit:text("unit").notNull(), ownerId:text("owner_id"), publicArea:text("public_area"), verificationStatus:text("verification_status").notNull().default("PENDING"), verifiedBy:text("verified_by"), verifiedAt:text("verified_at"), expiresAt:text("expires_at"), createdAt:text("created_at"), updatedAt:text("updated_at").notNull() }, (table)=>[index("idx_resources_category_quantity").on(table.category,table.quantity),index("idx_resources_verification_expiry").on(table.verificationStatus,table.expiresAt)]);
export const resourceTransactions = sqliteTable("resource_transactions", { id:integer("id").primaryKey({autoIncrement:true}), resourceId:text("resource_id").notNull(), delta:integer("delta").notNull(), note:text("note").notNull(), actorId:text("actor_id").notNull(), createdAt:text("created_at").notNull() });
export const notifications = sqliteTable("notifications", { id:text("id").primaryKey(), userId:text("user_id").notNull(), title:text("title").notNull(), body:text("body").notNull(), type:text("type").notNull(), readAt:text("read_at"), createdAt:text("created_at").notNull() }, (table)=>[index("idx_notifications_user_read").on(table.userId,table.readAt)]);
export const reports = sqliteTable("reports", { id:text("id").primaryKey(), requestId:text("request_id").notNull(), reporterId:text("reporter_id").notNull(), reason:text("reason").notNull(), status:text("status").notNull(), createdAt:text("created_at").notNull(), reviewedAt:text("reviewed_at") });
export const auditLogs = sqliteTable("audit_logs", { id:integer("id").primaryKey({autoIncrement:true}), actorId:text("actor_id").notNull(), action:text("action").notNull(), entityType:text("entity_type").notNull(), entityId:text("entity_id").notNull(), metadata:text("metadata").notNull(), createdAt:text("created_at").notNull() }, (table)=>[index("idx_audit_logs_created").on(table.createdAt),index("idx_audit_logs_entity").on(table.entityType,table.entityId)]);
export const rateLimits = sqliteTable("rate_limits", { key:text("key").primaryKey(), windowStartedAt:integer("window_started_at").notNull(), count:integer("count").notNull() });
export const uploadedFiles = sqliteTable("uploaded_files", { key:text("key").primaryKey(), requestId:text("request_id").notNull(), ownerId:text("owner_id").notNull(), contentType:text("content_type").notNull(), sizeBytes:integer("size_bytes").notNull(), createdAt:text("created_at").notNull() });
export const adminCredentials = sqliteTable("admin_credentials", { userId:text("user_id").primaryKey(), adminLoginId:text("admin_login_id").notNull(), passwordSalt:text("password_salt").notNull(), passwordHash:text("password_hash").notNull(), passwordIterations:integer("password_iterations").notNull(), createdAt:text("created_at").notNull(), updatedAt:text("updated_at").notNull() }, (table)=>[uniqueIndex("idx_admin_credentials_login_id").on(table.adminLoginId)]);
export const adminSessions = sqliteTable("admin_sessions", { tokenHash:text("token_hash").primaryKey(), userId:text("user_id").notNull(), expiresAt:text("expires_at").notNull(), createdAt:text("created_at").notNull(), lastSeenAt:text("last_seen_at").notNull() }, (table)=>[index("idx_admin_sessions_user_expires").on(table.userId,table.expiresAt)]);
export const userSessions = sqliteTable("user_sessions", { id:text("id").primaryKey(), userId:text("user_id").notNull(), tokenHash:text("token_hash").notNull(), createdAt:text("created_at").notNull(), expiresAt:text("expires_at").notNull(), lastUsedAt:text("last_used_at").notNull(), userAgent:text("user_agent").notNull() }, (table)=>[uniqueIndex("idx_user_sessions_token").on(table.tokenHash),index("idx_user_sessions_user_expires").on(table.userId,table.expiresAt)]);
export const passwordResetTokens = sqliteTable("password_reset_tokens", { id:text("id").primaryKey(), userId:text("user_id").notNull(), tokenHash:text("token_hash").notNull(), createdAt:text("created_at").notNull(), expiresAt:text("expires_at").notNull(), consumedAt:text("consumed_at") }, (table)=>[uniqueIndex("idx_password_reset_hash").on(table.tokenHash),index("idx_password_reset_user").on(table.userId)]);
export const emailVerificationTokens = sqliteTable("email_verification_tokens", { id:text("id").primaryKey(), userId:text("user_id").notNull(), tokenHash:text("token_hash").notNull(), createdAt:text("created_at").notNull(), expiresAt:text("expires_at").notNull(), consumedAt:text("consumed_at") }, (table)=>[uniqueIndex("idx_email_verification_hash").on(table.tokenHash),index("idx_email_verification_user").on(table.userId)]);
